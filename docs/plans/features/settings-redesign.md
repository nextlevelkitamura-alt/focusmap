# 設定画面リデザイン

> 作成: 2026-02-20 | 想定規模: 中（4-6ステップ）

## 目標

スマホファーストで使いやすい設定画面を作成し、PC版にも統合する。

---

## 設定項目一覧

### カレンダー連携
| 項目 | 種別 | 備考 |
|------|------|------|
| Google連携 ON/OFF | トグル | 既存 |
| 連携状態表示 | 表示 | 既存（連携中/期限切れ/未連携） |
| 最終同期時刻 | 表示 | 既存 |
| 手動同期ボタン | ボタン | 新規（更新アイコン） |
| 自動取り込み ON/OFF | トグル | 新規 |
| 取り込み期間 | セレクト | 新規（1週間/1ヶ月/3ヶ月） |
| 取り込むカレンダー | 複数選択 | 新規 |

### アカウント
| 項目 | 種別 | 備考 |
|------|------|------|
| ログアウト | ボタン | 新規 |
| アカウント削除 | ボタン | 新規（確認ダイアログ付き） |

### 表示
| 項目 | 種別 | 備考 |
|------|------|------|
| ダークモード | セレクト | 新規（システム/ライト/ダーク） |

### 通知（既存維持）
| 項目 | 種別 | 備考 |
|------|------|------|
| ブラウザ通知許可 | 既存 | |
| タスク/イベント通知 | 既存 | |

---

## 実装しないもの

- 終日イベントの取り込み設定（スキップ固定でOK）
- デフォルト見積もり時間
- デフォルト優先度

---

## UI構成

### モバイル（`/dashboard/settings`）

```
┌─────────────────────────────┐
│ ← 設定                      │
├─────────────────────────────┤
│                             │
│ 📅 カレンダー連携           │
│ ├─ Google連携      [ON/OFF] │
│ ├─ 最終同期: 2/20 10:30     │
│ ├─ 手動同期        [🔄]     │
│ │                           │
│ │ 自動取り込み    [ON/OFF]  │
│ │ ├─ 期間: [1ヶ月 ▼]       │
│ │ └─ カレンダー選択...      │
│                             │
├─────────────────────────────┤
│                             │
│ 🔔 通知設定                 │
│ ├─ ブラウザ通知    [許可]   │
│ ├─ タスク開始     [ON/OFF]  │
│ └─ ...                      │
│                             │
├─────────────────────────────┤
│                             │
│ 🎨 表示                     │
│ └─ テーマ: [システム ▼]     │
│                             │
├─────────────────────────────┤
│                             │
│ 👤 アカウント               │
│ ├─ メール: user@example.com │
│ ├─ [ログアウト]             │
│ └─ [アカウント削除] 🔴      │
│                             │
└─────────────────────────────┘
```

### PC（ヘッダーに設定アイコン追加）

- ヘッダー右端に ⚙️ アイコン
- クリックで `/dashboard/settings` へ遷移

---

## Phase 構成

### Phase 1: UI構築（モバイル）
- [x] 設定ページのレイアウト再構成
- [x] カレンダー連携セクションの拡張
- [x] 表示セクション（ダークモード）追加
- [x] アカウントセクション追加

### Phase 2: カレンダー設定の実装
- [x] 手動同期ボタン実装
- [x] 自動取り込み ON/OFF（localStorage）
- [x] 取り込み期間選択（1週間/1ヶ月/3ヶ月）
- [x] 取り込むカレンダー選択

### Phase 3: アカウント機能の実装
- [x] ログアウト機能
- [x] アカウント削除機能（確認ダイアログ + API）

### Phase 4: PC統合
- [ ] ヘッダーに設定アイコン追加
- [ ] PC版レイアウト調整

---

## DB変更

### user_settings テーブル（新規）

```sql
CREATE TABLE user_settings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users NOT NULL UNIQUE,

  -- カレンダー設定
  auto_import_enabled BOOLEAN DEFAULT true,
  import_period_days INTEGER DEFAULT 30,  -- 7, 30, 90
  import_calendar_ids TEXT[] DEFAULT '{}', -- 空配列 = 全カレンダー

  -- 表示設定
  theme VARCHAR(20) DEFAULT 'system',  -- 'system', 'light', 'dark'

  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
```

---

## 技術メモ

- テーマ設定は `next-themes` を使用（既存導入済み）
- カレンダー選択は `useCalendars` から取得
- アカウント削除は Supabase Auth API を使用
