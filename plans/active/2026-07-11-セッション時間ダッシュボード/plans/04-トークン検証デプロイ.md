親計画: ../program.md ／ 分類: repo ／ 種別: 新規作成 ／ 担当: 私（Claude本体）＋人間ゲート

# 04 トークン・検証・デプロイ

## 目的

トークンを安全に設置し、実データで数字の正しさを検証してから本番へ出す。

## 内容

1. トークン発行・設置（値は表示・記録・commitしない）:
   - board用**読み取り専用**トークン → ローカル `.env.local` の `PERSONAL_OS_BOARD_*`。
   - inbox用トークン → `PERSONAL_OS_INBOX_*`（server actionのみが使用・クライアントへ出さない）。
   - 本番: Cloud Run env / GitHub Secrets へ同名で（人間の画面操作 or 承認下CLI）。
2. 検証（localhost:3001）:
   - 本日サマリ・目標別・⏸滞留の数字が保存SQL3本の結果と一致。
   - ＋ボタン→inbox→未着手表示の往復。goal-add（板側）→画面反映。
   - サブ稼働（03修正後のsub-start/end）がサブ実行・稼働中N体に出る。
   - 既存監視画面（focusmap-codex-monitoring）に回帰なし。ダーク/ライト・モバイル確認。
3. デプロイ: feat/sessions-dashboard → main へマージし push（**=Cloud Run本番デプロイ・人間ゲート**）。

## 実施結果（2026-07-11）

- トークン: ローカル `.env.local`（board読み取り専用＋inbox・600権限・gitignore済み）、本番 GitHub Secrets 2本（`PERSONAL_OS_BOARD_AUTH_TOKEN`=読み取り専用新規発行 / `PERSONAL_OS_INBOX_AUTH_TOKEN`）。値はどこにも表示・記録していない。workflowへenv4行追加（URLは平文・トークンはSecrets参照）。
- 検証: 本日サマリの数字が保存SQLと一致（run145/sub8/sessions9完全一致・waitの差分はnow止め区間の経過分で説明可能）。＋ボタン相当（goal-add→inbox→画面「未着手」）往復OK。sub-start→Tursoイベント到達OK。過去日ゴースト混入を発見し当日絞り込みで修正（ea40eb9f）。
- デプロイ: local main取り込み（a9cbf697）→ ユーザーGO → `git push origin main`（4b473743..a9cbf697・以前からの未push10コミットを含む14本）→ GitHub Actions run 29136607859 **success** → 本番ルート応答確認（未認証307→/login＝新リビジョン稼働）。
- 残: 人間の実機確認（本番UIの見た目・ダーク/ライト・モバイル）→ OKで本program閉じ。

## 完了条件（レビュー項目）

- [ ] トークン2本が設置され、値がコード・ログ・commit・チャットに現れていない。
- [ ] 検証4点（数字一致・＋往復・サブ反映・回帰なし）の結果が本mdに追記されている。
- [ ] 人間のGOを得て main push（本番反映）が完了している。
